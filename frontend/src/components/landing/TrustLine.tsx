import { SectionShell } from "./SectionShell";

export function TrustLine() {
  return (
    <SectionShell className="border-y border-white/[0.06] bg-white/[0.02] py-8">
      <p className="text-center text-sm font-medium tracking-wide text-slate-500">
        Trusted by candidates preparing for top tech interviews
      </p>
    </SectionShell>
  );
}
